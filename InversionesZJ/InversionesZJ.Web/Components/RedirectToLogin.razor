@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Si la página exige login y no hay sesión, mandamos al usuario al login.
        Navigation.NavigateTo("/login", forceLoad: true);
    }
}
